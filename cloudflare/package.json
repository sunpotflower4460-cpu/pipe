{
  "name": "code-memo-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers MVP for Code Memo",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
